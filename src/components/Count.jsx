export default function Count({ count }) {
    return (
        <div className="count die rounded-2 die__active fs-5 d-flex justify-content-center align-items-center fw-semibold">
            {count}
        </div>
    )
}